# 安全策略

## 报告漏洞

不要在公开 Issue 中披露可利用细节、密钥或客户数据。请通过仓库维护方的私密安全渠道报告，并包含影响版本、复现步骤、租户边界和建议缓解措施。

## 安全边界

- 身份由后端验证 JWT/JWKS；前端角色仅用于显示，不构成授权。
- 组织上下文由认证信息和服务端策略推导；RLS 是数据库最终边界。
- Service Role 密钥只允许存在于后端运行时。
- Agent 工具在执行前经过 RBAC、策略守卫、提示注入检查和必要的 HITL。
- 审批、会员、财务和批量外发等操作必须审计并具备幂等性。

## 密钥响应

发现泄露时立即轮换密钥、撤销会话、检查审计日志、评估跨租户影响，并在事故记录中保留时间线。不要只删除 Git 中的字符串而继续使用旧密钥。

支持版本以当前生产分支为准。历史版本仅在有明确维护合同的情况下回补安全修复。
