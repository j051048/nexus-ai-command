# Development & Testing dependencies
# Install: pip install -r requirements-dev.txt
# (includes all production deps via -r requirements.txt)

-r requirements.txt

ruff==0.8.6
black==24.10.0
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
pytest-timeout==2.3.1
pytest-retry==1.6.3
httpx==0.28.1

# RAG Evaluation (optional — pulls massive ML libs)
# ragas>=0.2.14
# datasets>=3.0.0
hypothesis>=6.100.0
respx==0.21.1